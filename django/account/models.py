from django.db import models
from django.contrib.auth.models import User

# class Statistics(models.Model):
#     user = models.OneToOneField(User, on_delete=models.CASCADE)
#     late_num = models.DecimalField(max_digits=5, decimal_places=2, default=0)
#     tot_interview = models.IntegerField(default=0)
#     avg_rate = models.DecimalField(max_digits=5, default=0)
